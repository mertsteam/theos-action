name: Build AsuraBypass

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Theos
        uses: mertsteam/theos-action@v2.4.6   # kendi fork'unu kullan
        with:
          theos-src: 'theos/theos'
          theos-branch: 'main'
          sdks-src: 'theos/sdks'
          sdks-branch: 'master'
          libgcuniversal: 'true'
          altlist: 'true'

      - name: Build
        run: |
          make clean
          make package FINALPACKAGE=1
          make clean package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=rootless   # rootless için
          # İstersen roothide de ekle: THEOS_PACKAGE_SCHEME=roothide

      - name: Upload dylib
        uses: actions/upload-artifact@v4
        with:
          name: AsuraBypass-Artifacts
          path: packages/*.deb
